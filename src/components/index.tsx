import * as React from "react"
import * as ReactDOM from "react-dom"

import { MainContent } from "./mainContent"

ReactDOM.render(
    <MainContent compilerVersion="1.1.0" />,
    document.getElementById("main")
);